- name : install apache2
  package:
    - name : "{{ pkg }}"
    - state : latest
  when : ansible_os_family == ""Debian""
- name : Deploy static website on apache2
  copy:
    - src : "{{srcfile}}"
    - dest : "{{destfile}}"
  notify :
    - start apache2