- name: Install Apache2
  hosts: all
  become: true
  tasks:
    - name : Install apache2 on debian
      package:
        name: apache2
        state: latest
      when: ansible_os_family == "Debian"

    - name : Start apache2
      service:
        name: apache2
        state: started
        enabled: true